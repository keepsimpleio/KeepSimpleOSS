export interface WashStrokeProps {
  /** Index into the shared pigment cycle (viridian, ultramarine, sienna, ochre). */
  accent?: number;
  /** Base wash opacity; the default in the painter suits text backdrops. */
  alpha?: number;
  className?: string;
}
