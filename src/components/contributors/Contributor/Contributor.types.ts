export type ContributorProps = {
  name: string;
  role: string;
  japaneseLetter: string;
  isActive: boolean;
  socialLink?: string;
  isDarkTheme: boolean;
  isMobile?: boolean;
  socialLinkTxt?: string;
};
