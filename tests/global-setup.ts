// Playwright global setup. The config references this file; intentionally a
// no-op until there is shared setup work to do (auth state, seeded fixtures,
// etc.).
export default async function globalSetup(): Promise<void> {
  return;
}
